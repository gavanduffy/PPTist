[build]
  command = "npm run build"
  publish = "dist"

[dev]
  command = "npm run dev:full"
  port = 5173

[[redirects]]
  from = "/api/*"
  to = "http://localhost:5000/:splat"
  status = 200
  force = true
